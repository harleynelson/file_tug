name: realtor_assistant
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  speech_to_text: ^7.0.0
  provider: ^6.1.4
  google_sign_in: ^6.3.0
  googleapis: 13.1.0
  path_provider: ^2.1.5
  path: ^1.9.1
  file_picker: ^10.1.2
  flutter_contacts: ^1.1.9+2
  dialog_flowtter: ^0.3.3  
  flutter_dotenv: ^5.2.1
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  provider: ^6.1.4

flutter:
  uses-material-design: true
  assets:
  - assets/
